#!/command/execlineb -P
# mautrix-telegram bridge service

fdmove -c 2 1

# Wait for PostgreSQL to be ready
if { /usr/local/bin/wait-for-postgres.sh }

/usr/bin/mautrix-telegram -c /data/bridges/telegram/config.yaml
